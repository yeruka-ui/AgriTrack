<Page
    x:Class="agrify.Pages.SalesReport"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:agrify.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Background="#FF2D2D30">

    <Grid Padding="48" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="Sales Entry"
                   Foreground="White"
                   FontSize="20"
                   FontWeight="Bold"
                   Grid.Row="0" Margin="0,0,0,6"/>

        <!-- Row: Controls -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="1.6*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="0.9*"/>
            </Grid.ColumnDefinitions>

            <!-- Item Type -->
            <StackPanel Grid.Column="0">
                <TextBlock Text="ITEM TYPE" Foreground="LightGray" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <ComboBox x:Name="ItemTypeComboBox" MinWidth="180" SelectionChanged="ItemTypeComboBox_SelectionChanged">
                    <ComboBoxItem Content="Produce" />
                    <ComboBoxItem Content="Livestock" />
                </ComboBox>
            </StackPanel>

            <!-- Item Select -->
            <StackPanel Grid.Column="1" Margin="12,0,0,0">
                <TextBlock Text="ITEM" Foreground="LightGray" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <ComboBox x:Name="ItemComboBox" HorizontalAlignment="Stretch" SelectionChanged="ItemComboBox_SelectionChanged"
                          PlaceholderText="Select item..." />
            </StackPanel>

            <!-- Quantity -->
            <StackPanel Grid.Column="2" Margin="12,0,0,0">
                <TextBlock Text="QUANTITY" Foreground="LightGray" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <NumberBox x:Name="QuantityNumberBox" Minimum="1" ValueChanged="QuantityNumberBox_ValueChanged"/>
            </StackPanel>

            <!-- Unit Price -->
            <StackPanel Grid.Column="3" Margin="12,0,0,0">
                <TextBlock Text="UNIT PRICE" Foreground="LightGray" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <NumberBox x:Name="UnitPriceNumberBox" Minimum="0" ValueChanged="UnitPriceNumberBox_ValueChanged" />
            </StackPanel>

            <!-- Set Default Price -->
            <StackPanel Grid.Column="4" Margin="12,0,0,0" VerticalAlignment="Bottom">
                <Button x:Name="SetDefaultPriceButton" Content="Set As Default Price" Click="SetDefaultPriceButton_Click" />
            </StackPanel>
        </Grid>

        <!-- Row: Total + Notes -->
        <Grid Grid.Row="2" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="NOTES (OPTIONAL)" Foreground="LightGray" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <TextBox x:Name="NotesTextBox" Height="72" AcceptsReturn="True" TextWrapping="Wrap" Background="#FFD9FFD9"/>
            </StackPanel>

            <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBlock Text="TOTAL" Foreground="LightGray" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <TextBlock x:Name="TotalTextBlock" Text="0.00" Foreground="White" FontSize="18" FontWeight="Bold" HorizontalAlignment="Right" />
                <Button x:Name="SubmitSaleButton" Content="RECORD SALE" Width="180" Margin="0,12,0,0" Click="SubmitSaleButton_Click"/>
                <TextBlock x:Name="SaleMessageTextBlock" Foreground="LightGreen" FontWeight="SemiBold" TextWrapping="Wrap" MaxWidth="220" Margin="0,8,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Row: Search / Filters -->
        <Grid Grid.Row="3" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SalesSearchBox" PlaceholderText="Search sales..." MinWidth="300" Grid.Column="0" TextChanged="SalesSearchBox_TextChanged"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                <TextBlock Text="From:" Foreground="LightGray" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <DatePicker x:Name="FromDatePicker" DateChanged="FilterDateChanged"/>
                <TextBlock Text="To:" Foreground="LightGray" VerticalAlignment="Center" Margin="8,0,4,0"/>
                <DatePicker x:Name="ToDatePicker" DateChanged="FilterDateChanged"/>
                <Button Content="Apply" Click="ApplyFilterButton_Click"/>
            </StackPanel>
        </Grid>

        <!-- Row: DataGrid of Sales -->
        <controls:DataGrid x:Name="SalesDataGrid"
                           Grid.Row="4"
                           AutoGenerateColumns="False"
                           IsReadOnly="True"
                           GridLinesVisibility="Horizontal"
                           HorizontalScrollBarVisibility="Auto">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Date" Binding="{Binding SaleDate}" Width="150"/>
                <controls:DataGridTextColumn Header="ItemType" Binding="{Binding ItemType}" Width="120"/>
                <controls:DataGridTextColumn Header="ItemName" Binding="{Binding ItemName}" Width="*"/>
                <controls:DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="80"/>
                <controls:DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice}" Width="110" />
                <controls:DataGridTextColumn Header="Total" Binding="{Binding TotalAmount}" Width="120"/>
                <controls:DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="200" />
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </Grid>
</Page>
